package com.itheima.redbaby.ui;

import com.itheima.redbaby.ui.BaseView;
import com.itheima.redbaby.ui.Context;
import com.itheima.redbaby.ui.OnClickListener;

public class CheckUpdateView extends BaseView implements OnClickListener {
	
	public CheckUpdateView(Context context) {
		super(context);
	}

	/**
	 * 获取中间容器
	 */
	@Override
	protected void loadMiddleLayout() {
		middleView = View.inflate(context, R.layout.dl_count_check_update, null);
	}

	/**
	 * 获取对象
	 */
	@Override
	protected void findViewById() {

//		middleView = (ViewGroup) View.inflate(context, R.layout.dl_login, null);
//
//		username = (EditText) middleView
//				.findViewById(R.id.dl_user_login_username);
//		password = (EditText) middleView
//				.findViewById(R.id.dl_user_login_password);
//		login = (Button) middleView.findViewById(R.id.dl_user_login);
//		register = (Button) middleView.findViewById(R.id.dl_user_login_regist);
	}

	/**
	 * 设置监听
	 */
	@Override
	public void setListener() {

//		login.setOnClickListener(this);
//		register.setOnClickListener(this);
	}

	/**
	 * 点击事件
	 */
	@Override
	public void onClick(View view) {
		
		
	}

	@Override
	protected void processEngine() {
		
		
	}

	@Override
	protected int getID() {
		return 0;
	}

	/**
	 * 用户信息判断
	 * 
	 * @return
	 */
	private boolean checkUserInfo() {
		return true;
	}

}